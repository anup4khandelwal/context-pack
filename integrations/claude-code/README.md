# Claude Code plugin

## Install

```bash
cp -R integrations/claude-code ~/.claude/plugins/context-pack
```

## Usage

```
pack.sh "<task>" [budget]
```

The script generates `.context-pack/` in the current repo.
